const express = require('express')
const app = express()
const port = 3001node

app.get('/', (req, res) => {
  res.send('Hello GET!')
})

app.post('/',(req, res))  => {
  res.send('Hello POST!')
})

app.listen(port, () => {
  console.log(`Example app listening at http://localhost:${port}`)
})
